posh 
	 fs = new  formdata (form)
 opent {
	模式，
	数据 body= fs，	
	}

fetch(url,opent）
	

上午 cb 
	创建 sc link 等超链接
	fech调用 url 尾缀跟callback  =  自定义函数 
	后端res.wire  自定义函数（内容）
 缺点 仅支持get

	
下午
xsh 4 200 
set open

cosr  
	feach发送自己服务器 自己服务器发送别人的服务器 别人的返回   自己的返回到feach 上